# import（取り込み元・個人データ）

`import-profile` スキルで `profile.md` に取り込む、**既存のスキルシート/職務経歴書**を置くフォルダ。

- ここに置くファイルは個人情報を含むため、この README 以外は **.gitignore 済み**（コミットされない）。
- Markdown / Excel / Word / PDF など何でも可。
- 取り込みは `profile.md` への転記のみで、ここのファイルは書き換えない。
